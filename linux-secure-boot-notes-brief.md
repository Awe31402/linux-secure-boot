# 讀書筆記 Brief — Secure Boot Encryption with Linux

- **書**：Rodolfo Giometti, *Secure Boot Encryption with Linux: Implementation for Embedded Developers* (Apress, 2026), 252 頁
- **檔案**：`/home/awe/security-and-crytography/linux-secure-boot.pdf`
- **定案日期**：2026-08-28
- **最後修訂**：2026-08-29（第三次：加入附錄 B，見第 3、4、5 節）
- **階段**：第 1–4 章、附錄 B 與網頁版皆已完成。第 3 章逐步操作與附錄 A 仍在範圍外。

---

## 1. Purpose

給自己的入門地圖。目標是讀完之後，能用自己的話講出「Linux 上的 secure boot
從開機到進系統，每一棒是誰、傳什麼、憑什麼相信下一棒」。

不是操作手冊，現階段不會照著做。它是之後回頭讀第 3 章逐步操作與兩個附錄時
看得懂的底子。因此筆記偏重**為什麼**，不是把指令抄下來。

> 第 4 章（2026-08-29 加入）延續同一個目的：它回答前三章一直沒回答的
> 「**鑰匙到底是怎麼生出來的**」，以及「**這樣鎖住裝置，會不會違反 GPLv3**」。
> 兩題都屬於「講得出來」的範圍，不是「做得出來」。

## 2. Situation

- 這本書講的是**嵌入式 ARM 板子**的 secure boot：U-Boot、Yocto、i.MX8 / i.MX9、
  STM32MP1、CAAM 加密硬體、燒 fuse。
- **不是**一般 PC 那套 UEFI + shim + 微軟簽章。網路上搜「Linux secure boot」
  大部分是後者，兩者觀念相通但零件完全不同 —— 筆記要講清楚這個差別，
  避免日後查資料混淆。
- 作者假設讀者是資深嵌入式開發者，讀者本人不是。書裡當常識跳過的名詞
  （U-Boot SPL、fuse、CAAM 等）筆記要補一句白話解釋。

## 3. Inputs

- **主要來源**：`/home/awe/security-and-crytography/linux-secure-boot.pdf`，僅此一本。
- **範圍（骨幹版）**：
  - 第 1 章 Linux Cryptography — 書頁 p.1–75
  - 第 2 章 What Secure Boot Is (and What It Is NOT) — 書頁 p.77–107
  - 第 3 章 概念部分 — 書頁 p.109–118（Rescue schema 是什麼）、
    p.161–165（A/B schema 是什麼），不含逐步操作
- **範圍增補（2026-08-29）**：
  - 第 4 章 Final Considerations — 書頁 p.191–204（PDF p.208–221）**整章**
    - 4.1 Generating Keys for Crypting and Signing（p.191）
    - 4.1.1 The Fuse-Centric Approach（p.192，範例平台 STM32MP1x）
    - 4.1.2 The Hybrid Approach（p.196，範例平台 i.MX / AHAB）
    - 4.2 Licensing Issues（p.201，GPLv3 第 6 條與 Tivoization）
    - 4.3 Summary（p.204）
- **範圍增補（2026-08-29 第三次）**：
  - 附錄 B Securing Secrets Across Booting Stages — 書頁 p.217–236
    （PDF p.233–252）**整篇**
    - 前言：階段之間交換資訊為什麼是破口（p.217–222）
    - B.1 Fixing the Kernel Command line（p.223–226）
    - B.2 Embedding Secrets in the Kernel（p.226–236）
  - **納入理由**：它補的是 §9 留下的洞（U-Boot 環境變數與 kernel command line
    是明文），是筆記自己標記為「特別值得補」的那一項。
- **不使用**：不上網查、不引用其他書或部落格。筆記要能對回書上頁碼。
- **缺口處理**：書上沒寫清楚的地方，明寫「書上沒講」，
  **不得自行補一個聽起來合理的答案**。要能分辨哪些是書上說的、哪些是缺口。
- **原文引用（2026-08-29 新增）**：每節在出處行之前，附一段 📄 **原文** ——
  直接引書上的英文原句，**不翻譯、不改寫**，並標明書頁與 PDF 頁。
  - 目的：讀者可以自己核對「筆記講的」跟「書上寫的」是不是同一件事。
    這是「缺口處理」那條規則的實際檢查手段。
  - 選句標準：挑該節**論點的來源句**，不是隨便一段。重點多的節可放兩段。
  - 長度：一段以 3–6 句為上限；中間省略用 `[...]` 標出。
  - 抽字失敗（PDF 排版導致漏字、亂序）時，**寧可不引，也不補字**；
    引不到就在筆記裡寫明「PDF 抽不出來，請翻書頁 X」。

## 4. Limits

- 不要變成操作手冊。指令、程式碼只在「不看就講不清楚概念」時放，
  且僅放最小片段，不整段貼。
- 不延伸到範圍以外：第 3 章逐步操作、附錄 A，一律不碰。
- **第 4 章照舊套用上面那條「不要變成操作手冊」**（2026-08-29 決定）。
  4.1 節書上給了 STM32MP1x 與 i.MX 兩套完整指令序列 ——
  筆記**只講兩條路線的差別、為什麼要分兩種、鑰匙最後躺在哪**，
  指令只留最小片段當「長什麼樣」，不逐步解說參數。
  > 代價已知：日後真的要產鑰匙時，還是得翻書頁 192–200。這是有意識的取捨。
- **附錄 B 同樣套用「不要變成操作手冊」**（2026-08-29 決定）。
  B.2 的 `key-injector` 核心模組原始碼書上拆成好幾段講 ——
  筆記**只講它做了什麼、為什麼這樣做、以及書上自己說它為什麼不夠**，
  不逐段解說 C 程式碼。
  > 但**攻擊示範的指令要留**（`fw_setenv`、`nm`、`objdump`）：
  > 那幾行不是操作步驟，是這一篇的論點本身 —— 不看就不知道洞有多好鑽。
- 不做技術選型建議（例如「你應該用 A/B 方案」）。現階段只要搞懂它們是什麼。
- 不為完整而完整。第 1 章的 API 細節若對理解主線無幫助就跳過，
  不必每節都交代。

## 5. Done-when

- **成品**：
  - `linux-secure-boot-notes.md` — 主筆記
  - `linux-secure-boot-notes.html` — 網頁閱讀版（由 md 建置產生，見第 7 節）
- **每節模板**：這在解決什麼問題 → 怎麼運作（白話）→ 名詞小抄
  → 📄 原文引用 → 📖 書頁 ＋ PDF 頁 ＋ 可點的 PDF 連結
- **份量**：第 1–3 章約 8–12 頁 A4；第 4 章增補再約 2–3 頁
- **完成標準**：
  1. 不看書即可把開機信任鏈從第一棒講到掛載 root filesystem（第 1–3 章）
  2. 不看書即可講出 fuse-centric 與 hybrid 兩種產鑰匙路線的差別，
     以及 Secure Boot 與 GPLv3 為什麼會打架（第 4 章）
  3. 不看書即可說出：攻擊者拿到 root 之後，如何靠改 U-Boot 環境變數
     繞過 init 拿到鑰匙，書上兩種修法各自擋掉什麼、又各自剩下什麼洞（附錄 B）
- **第一個檢查點**：先交**大綱**（每節標題 + 2–3 句說明本節會講什麼），
  確認後才寫全文
- ~~**選配**：全文完成後，可另外發一份瀏覽器可看的網頁版~~ → 已完成

---

## 6. 頁碼對照：書頁 ↔ PDF 頁

**這本 PDF 的書頁與 PDF 頁差值不是固定的**，每章開頭會少一頁。
逐頁讀出 PDF 上印的頁碼後，實際對應如下：

| 書頁 | PDF 頁 | 位移 |
|---|---|---|
| 1–75 | 21–95 | +20 |
| 77–107 | 96–126 | +19 |
| 109–189 | 127–207 | +18 |
| 191–215 | 208–232 | +17 |
| 217–236 | 233–252 | +16 |

書頁 **76、108、190、216 在這個 PDF 裡不存在**（章節分隔頁未編號）。

> ⚠️ **教訓**：第一次只抽樣了幾頁就推論出「差值固定 +20」，
> 結果第 2、3 章的頁碼全錯。
> **規則：頁碼對照必須逐頁掃過整本 PDF 驗證，不得由少數樣本外推。**

重建對照表的作法：

```bash
for p in $(seq 21 252); do
  n=$(pdftotext -f $p -l $p linux-secure-boot.pdf - 2>/dev/null \
      | grep -E '^[0-9]{1,3}$' | tail -1)
  echo "$p ${n:-NONE}"
done
```

（每頁的頁碼印在該頁文字流的最後，所以取最後一個獨立的數字行。）

## 7. 網頁版怎麼建

網頁版是**從 markdown 建置產生的**，不是手寫的第二份內容 ——
所以 **markdown 是唯一真相來源，改完一定要重跑建置**。

- 建置腳本：`tools/build_notes_html.py`
- 版型樣板：`tools/notes_template.html`
- 產出：`linux-secure-boot-notes.html`（單一檔案，無執行期 JS 相依）

改完 markdown 後重跑：

```bash
python3 tools/build_notes_html.py
```

引言區塊靠開頭的符號分類上色：
`⚠️` 警告 · `🎯` 重點 · `📌` 書上明說的缺口 · `💡` 訣竅 · `ℹ️` 補充 · `📄` 原文引用。

> ⚠️ **相鄰的引言區塊會被 python-markdown 黏成一塊**，
> 分類會因此跑掉（例如兩段原文變成一段、上色錯誤）。
> 建置腳本裡的 `split_adjacent_quotes()` 會在兩個 `>` 區塊之間
> 自動插入 `<!-- -->` 隔開，所以手寫 markdown 時不用自己補。
> **但如果換掉建置流程，這件事要記得重做。**

---

## 已排除的選項（避免重複討論）

- 操作導向筆記（手上有板子、照著做）— 否決，現階段先求理解
- ~~全書涵蓋（含 Ch3 細節、Ch4、兩附錄）— 否決，先打底，之後想要再補~~
  → **第 4 章與附錄 B 已於 2026-08-29 納入範圍**。
  第 3 章逐步操作與附錄 A（防拆偵測）仍在範圍外。
- 把第 4 章 4.1 寫成可照做的產鑰匙流程 — 否決（2026-08-29），
  理由見第 4 節：與「不要變成操作手冊」衝突，且指令綁死廠商，換平台就用不上。
