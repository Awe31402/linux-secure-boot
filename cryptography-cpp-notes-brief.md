# 讀書筆記 Brief — Practical Cryptography

- **對象書**：`cryptography-cpp.pdf`
  — Saiful Azad、Al-Sakib Khan Pathan 編，*Practical Cryptography: Algorithms
  and Implementations Using C++*，CRC Press / Auerbach，2015
  （ISBN 978-1-4822-2890-8，PDF 共 **359 頁**，正文 **14 章**）
- **產出檔案**：`cryptography-cpp-notes.md`（**一整份持續長大**，每輪往下接）
- **定案日期**：2026-09-01
- **階段**：第一輪 —— 本 brief ＋ 骨架，尚未填內容
- **本輪範圍**：**第 1～3 章**（書頁 1–44 ｜ PDF 頁 23–66）

---

## 1. Purpose

**靠這份筆記把《Practical Cryptography》讀懂 —— 筆記是主要閱讀物，書是查證來源。**

成功長這樣：**翻到書上任何一章，我說得出**

- 這章在解**什麼問題**，
- 它跟**前面哪一章**接得上，
- 以及書上那段 **C++ 在做我剛剛講的哪一步**。

定位是「**講得出來**」，不是「**寫得出來**」——
**不以自己實作 AES 為目標**，不做習題，不做效能調校。

**不服務課程考試。** repo 裡的 [`crypto-course-notes.md`](./crypto-course-notes.md)
是另一門課的筆記，兩份**平行存在、互相連結**，但這份不去猜老師會考什麼、
不排考前重點、不跟課程進度對齊。取捨標準只有一個：**這段對理解這本書有沒有幫助。**

## 2. Situation

### 這本書不是為零基礎寫的，而且是多作者合輯

書自己的 Preface（書頁 x）在 "What This Book Is Not For" 底下明講：
不適合完全在相關領域之外的讀者，預期你有計算機、網路、程式與資安的基礎，
而且「may not contain the meanings and details of each technical term」。

而且這是**編輯合輯**，14 章由不同團隊撰寫 —— 第 5 章 DES、第 9 章 ECC、
第 13 章多核心加速是三組完全不同的作者。後果：

> **符號慣例、深淺、寫作品質跨章不一致。不要假設第 5 章的記號在第 9 章還通用。**
> 筆記遇到記號改變時要明講，不要幫書「統一」成看起來一致的樣子。

### 書有實質的內容缺口（已逐項核對全書文字，非印象）

| 缺什麼 | 全書核對結果 | 為什麼對初學者要緊 |
|---|---|---|
| MAC / HMAC | 只出現 **2 次**，還是在第 12 章附帶提到 | **Integrity 這條線在書裡是斷的** —— 只有 hash，沒有 MAC |
| AEAD / GCM / ChaCha20-Poly1305 | AEAD **0 次**、ChaCha **0 次**、GCM 2 次 | 這是現代對稱加密的**預設形態**，書裡等於不存在 |
| Diffie–Hellman | 20 次**全部是歷史敘事**（第 7 章講專利糾紛），沒有協定與數學 | 金鑰交換是密碼學骨架的一根支柱 |
| SHA-3 / Keccak | Keccak **0 次**，第 11 章只列到 SHA-2 家族 | 2015 年出版時 SHA-3 已標準化兩年 |
| 亂數產生（CSPRNG） | 無專章，random number 僅 7 次 | 實務上初學者最容易出事的地方 |
| 後量子 | **0 次** | 2015 年的書，可理解，但要標 |
| MD5 | 反過來，**有整整一章 24 頁**（第 10 章） | MD5 在 2004 就被攻破，這章是**歷史教材**而非可用技術 |

> **這不代表書選錯了。** 它在 DES / AES / RSA / ECC 的「逐步拆解 ＋ 可跑的 C++」
> 上很紮實，那正是純理論書沒有的。
> 但**筆記必須主動標出缺口**，否則讀完會以為自己有完整骨架，實際上缺了一半。

### 已知會誤導人的書內陳述（第一輪抽查就撞到）

- 書頁 3 說 stream cipher「**is not much used in modern cryptography**」——
  **錯的**。ChaCha20 是今天 TLS 與行動裝置上的主力。RC4 退場 ≠ 串流密碼退場。
- 第 2 章 Caesar 的**演算法描述與它自己的程式碼對不起來**（詳見 §3「已知落差」）。

### 第 12～14 章跟前 11 章不是同一種書

第 12 章 Identity-Based Cryptography、第 13 章 Symmetric Key Encryption
Acceleration on Heterogeneous Many-Core Architectures、第 14 章 Fast Hashing
in Data Streaming —— 這三章是**研究論文調性**，前置知識（雙線性配對、
OpenCL/CUDA、串流演算法）遠超過本份筆記的範圍。**列為非目標**（見 §4）。

### 讀者起點：一律從零開始

**不假設任何背景。** 連「什麼是加密」都重講一遍，用到的數學（模運算、
GF(2^8)、歐幾里得、Euler phi、橢圓曲線群）在**用到的當下就地補**。

已知代價，**已接受**：

- 篇幅明顯變大。
- 會跟 `crypto-course-notes.md`、`linux-secure-boot-notes.md` 有**重複段落**。

換來的是**筆記自包含**，讀的時候不用跳檔。相關的地方仍放連結，但**不用連結代替解釋**。

### 語言

**中文為主，專有名詞保留英文**（plaintext、ciphertext、block cipher、
key schedule、S-box、modulus…），因為書是英文的，查書、查索引時要對得上。

### repo 現況

`crypto-course-notes.md` 裡的 `📖 對照 HAC §x` 那些行，指向 commit f17a64e
已刪除的 `handbook-of-applied-cryptography.pdf`，**現在是壞的**。
**這輪不修**（見 §4），但記在這裡以免忘記。

## 3. Inputs

### 主要來源（唯一事實依據）

**`cryptography-cpp.pdf` 本身。** 每一句「書上說什麼、哪個函式對應哪一步、
哪一頁有什麼」都必須來自**實際翻過那幾頁**。

> ⚠️ **不准靠對密碼學的印象推測書上的內容。**
> 這是整份筆記最容易產生幻覺、而且**最難被察覺**的地方 ——
> 一個編得很像的頁碼或一句聽起來很合理的「書上說」，要翻過去才會發現是編的。

### 頁碼制度：書頁 + 22 = PDF 頁（**全書固定**）

與合併版的 HAC 不同，這是單一出版 PDF，位移不變。
14 章章名頁**已逐章核對**：

| 章 | 標題 | 書頁 | PDF 頁 | 本輪 |
|---|---|---|---|---|
| 1 | Basics of Security and Cryptography | 1 | 23 | ✅ |
| 2 | Classical Cryptographic Algorithms | 11 | 33 | ✅ |
| 3 | Rotor Machine | 35 | 57 | ✅ |
| 4 | Block Cipher | 45 | 67 | |
| 5 | Data Encryption Standard | 57 | 79 | |
| 6 | Advanced Encryption Standard | 91 | 113 | |
| 7 | Asymmetric Key Algorithms | 127 | 149 | |
| 8 | The RSA Algorithm | 135 | 157 | |
| 9 | Elliptic Curve Cryptography | 147 | 169 | |
| 10 | Message Digest Algorithm 5 | 183 | 205 | |
| 11 | Secure Hash Algorithm | 207 | 229 | |
| 12 | Identity-Based Cryptography | 225 | 247 | ❌ 非目標 |
| 13 | Symmetric Key Encryption Acceleration | 251 | 273 | ❌ 非目標 |
| 14 | Fast Hashing in Data Streaming | 299 | 321 | ❌ 非目標 |

**取頁碼注意**：每章章名頁上印的是章名而非頁首，上表取的是**章名頁**。

### 可以拿來當骨架起點的東西

每一章開頭都有作者寫的 **Contents**（含書頁碼）與 **Keywords** 清單。
**拿來當骨架的起點，但不照抄** —— 筆記的分節可以比書細
（書的第 1 章正文有一大段完全沒有編號）。

### 引用制度

沿用 [`linux-secure-boot-notes.md`](./linux-secure-boot-notes.md) 的 `📄 原文` 格式：

```
> 📄 **原文**　書 p.3 ｜ PDF p.25 ｜ [開啟](./cryptography-cpp.pdf#page=25)
>
> A stream cipher is a method of encrypting text [...] one bit at a time.
```

**只引一兩句。** 這是 CRC Press 2015 的**有版權書**（版權頁明列不得重製），
不是 HAC 那種免費授權，所以引用一律短、且必附頁碼指標。

### ⚠️ 已知落差：書上印出來的程式碼本身就不能編譯

**這一條在第一輪查證時被推翻過一次，結論比原本以為的嚴重。**

一開始用 `pdftotext` 抽書頁 13 的 Caesar 實作，看到 `charcaesar`、`c ! = toupper(c)`、
`inti`，判斷是文字抽取工具的問題。**後來依 brief 的規定去看渲染後的頁面，發現不是** ——
書上**印出來就是那樣**：

| 書上印的 | 應該是 | 性質 |
|---|---|---|
| `charcaesar(char c, int k)` | `char caesar(...)` | **該有的空格被吃掉** |
| `voidPutCharInVec ()`、`intnum;`、`for(inti = 0; ...)` | `void PutCharInVec()`、`int num;`、`int i` | 同上 |
| `c ! = toupper(c)` | `c != toupper(c)` | **不該有的空格被插進去，運算子被拆開** |
| `= =`、`> =`、`< =`、`+ =` | `==`、`>=`、`<=`、`+=` | 同上 |
| `“Plain: ”`、`‘c’` | `"Plain: "`、`'c'` | 直引號被排版成**全形彎引號** |

看起來是排版流程（InDesign 匯入）把原始碼的空白正規化了，**出版前沒有人把它編譯回去驗證**。
後果：**書上任何一段程式碼照抄都不會過編譯。**

> 這件事本身就是筆記要傳達的內容之一 —— 讀者照著打會卡住，而且會以為是自己打錯。
> 每章的 `💻 程式碼對照` 第一次出現時要提醒一次。

**只有一種破壞真的是抽取工具造成的**：識別字被跨行切開（文字層出現 `c = t` 換行 `oupper(c);`），
渲染後的頁面上是連著的。所以：

> **規定：程式碼一律看渲染後的頁面轉錄，不得直接採用文字層的輸出。**
> 轉錄時**照書原樣呈現**（保留 `charcaesar`、`! =` 這些），
> 再另外標一行說明正確寫法 —— 不要默默幫書修好，那會讓讀者以為書是對的。

### ⚠️ 已知落差：書的演算法描述與它自己的程式碼對不起來

同樣是 Caesar（書頁 12–14），至少三處：

| 書上的演算法（§2.1.1） | 書上的程式碼（§2.1.2） |
|---|---|
| Step 0：A = 1, B = 2 | `c - 65`，即 **A = 0** |
| Step 1：key 取 **1 到 25** | 檢查 `key < 1 \|\| key > 26`，**允許 26** |
| 只描述加解密公式 | 額外**用大小寫決定加密還是解密**（小寫輸入→加密，大寫→解密），這不是凱撒密碼的一部分，是這個 demo 的介面設計 |

> **這正是「程式碼當觀念的驗證」最有價值的地方。**
> 筆記要把這種落差**攤開來講**，而不是挑一邊寫、假裝它們一致。

### 次要來源

我自己的密碼學知識，**只**用於：

- 「從零開始」所需的基礎鋪陳（書略過的前置觀念）
- 標記書上**過時或錯誤**的陳述
- 補一句白話解釋

凡是這種段落**一律標** `⚠️ 本節不是書上的內容`，比照 `linux-secure-boot-notes.md`。

### 禁止

- **查不到就留白。** 不生一個看起來合理的答案填進去。
- 不引入書外的第三方來源（NIST SP、RFC、其他教科書）當事實依據 ——
  缺口只標記，不用外部來源補（見 §4）。

## 4. Limits

- **不逐頁翻譯。** 用自己的話重寫。引原文只引一兩句，標清楚是引用。
- **不整段抄程式碼。** 每個演算法最多引**關鍵的 10–15 行**
  （S-box 查表、模冪迴圈、金鑰排程核心之類），完整實作請翻書。
  既避免筆記變成抄書，也是版權考量。
- **不編譯、不執行、不除錯書上的程式碼。** 這輪不驗證它跑不跑得起來、不修 bug。
  看出明顯錯誤**只標一行**，不改寫成「正確版本」。
- **缺口只標記，不補。** §2 表列的 MAC/HMAC、AEAD、Diffie–Hellman、SHA-3、
  CSPRNG 等，在該出現的位置放一個 `⚠️ 書上沒有這個` 框：**缺什麼、為什麼要緊、
  之後去哪裡補**（一行就好）。**不寫成完整章節**，筆記的事實基礎完全鎖在書上。
- **不改書的章節順序。** 照 1 → 11 走，不重排成「我覺得比較好的學習路徑」。
- **不動現有檔案。** `crypto-course-notes.md`、`linux-secure-boot-notes.md`
  及兩份 brief 完全不碰。那些指向已刪除 HAC 的壞掉 `📖 對照` 行，**另案處理**。
- **不做第 12～14 章。** 只在筆記結尾放一節
  「這三章是什麼、什麼時候再回來讀」。
- **本輪不做 html / pdf。** 先把 md 寫對，之後再決定要不要跑
  `tools/build_notes_html.py`。
- **不做習題、不做效能調校、不做安全稽核建議。**

## 5. Done-when

### 檢查點（先做這個）

先產出 **brief（本檔）** ＋ **只有標題與一句話說明的骨架**，
給使用者看過點頭，**才**填內容。

> 方向錯了在骨架階段抓，代價是一句話；填完才抓，代價是重寫。

### 每一節的固定樣板

體例在第一輪定死，之後照套：

```
### §x.y 這節的標題

這節在解什麼問題 —— 白話一段

觀念：步驟拆解（從零開始，用到的數學就地補）

💻 **程式碼對照**：書上哪個函式做哪一步，引關鍵 10–15 行

> 📄 **原文**　書 p.xx ｜ PDF p.xx ｜ [開啟](./cryptography-cpp.pdf#page=xx)

⚠️（視情況）書上沒有這個／這段已過時／這段是我補的／書上前後不一致
```

每章結尾：**「這章的三句話重點」** ＋ **「本章名詞小抄」**（中英對照）。

### 完成的樣子（本輪）

- `cryptography-cpp-notes-brief.md` — 本規格檔
- `cryptography-cpp-notes.md` — 筆記本體，**只寫第 1～3 章**
  （書頁 1–44 ｜ PDF 23–66）
- 每節照上面的樣板，附 `📄 原文` 頁碼指標
- 第 2、3 章每個演算法都要有 `💻 程式碼對照`
- 書上演算法與程式碼對不起來的地方，**逐處攤開**
- 結尾一節：「第 12～14 章是什麼、什麼時候再回來讀」

### 不做

html、pdf、第 4 章以後的內容、修 `crypto-course-notes.md` 的壞連結。
